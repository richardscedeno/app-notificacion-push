import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jcedenio.notificaionesPush',
  appName: 'notificacionesPush',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
